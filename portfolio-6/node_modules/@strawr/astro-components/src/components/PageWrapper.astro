<div
  id="pageWrapper"
  class="max-w-screen-2xl mx-auto overflow-x-clip"
>
  <div id="pageContent" class="transition-transform duration-300">
    <slot name="header" />
    <div id="contentWrapper" class="transition-all duration-300">
      <slot />
    </div>
  </div>
</div>
<style is:global>
  @keyframes grow-progress {
    from {
      transform-origin: center 25%;
    }
    to {
      transform-origin: center 90%;
    }
  }
  #contentWrapper {
    animation: grow-progress linear forwards;
    animation-timeline: scroll();
    /* transform-origin: center calc(0%-25vw); */
  }
</style>
