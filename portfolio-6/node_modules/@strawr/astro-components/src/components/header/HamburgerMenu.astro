---
type Props = {
    variant?: 'appear' | 'backMiddleRotate' | 'backRotate' | 'middleRotate' | 'reveal' | 'rotate' | 'snap' | 'spin'
}
const { variant='appear' } = Astro.props;
const components = await Astro.glob('./hamburgerMenus/*.astro');

const Menu = components.find((el) => el.file.endsWith(`header/hamburgerMenus/${variant}.astro`))
// console.log("THE EGG!!!!!!!!", components)
---
<Menu.default/>