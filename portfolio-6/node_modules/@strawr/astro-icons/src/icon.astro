---
import { type IconName } from './iconsNameType';

type Props = {
    name: IconName;
}
const { name } = Astro.props;
const { default: innerHTML } = await import(`./optimizedSvgs/${name}.svg?raw`);
---
<Fragment set:html={innerHTML} />